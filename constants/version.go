package constants

var (
	// Version is the current version of serve
	Version = "unknown"
	// CommitHash is the current commit hash of serve
	CommitHash = "unknown"
	// BuildTime is the time when serve was built
	BuildTime = "unknown"
)
